node_modules/
dist/
build/
.astro/
.svelte-kit/
.env
.DS_Store

# Data fetched at build time
data/pkf/
data/BSB/
data/.fetch-tmp/
data/source-catalog.json

# Public data copied from bibel-wiki (fetched, not committed)
public/ALL-langs-data/
public/ALL-langs-compact.json
public/ALL-langs-mini.json
public/templates/*
# ...except the "test" template's video-scene timing JSON: small, text,
# and needed at build time + as a runtime fetch — see loadVideoManifest().
!public/templates/test
public/templates/test/*
!public/templates/test/timing

# Symlinks into data/ (resolved at runtime)
public/pkf
public/source-catalog.json

# Test-template videos (2GB+, staying local until external hosting is set up)
public/video/

# Example projects
example/

# Previous SvelteKit implementation snapshot
prev/

# Confirmed-dead code, moved out of src/ so it's obviously unused rather
# than looking like a live parallel implementation (mirrors original paths
# under bak/ — e.g. bak/src/lib/components/StoryReader.svelte).
bak/

# Claude Code
.claude/

# Local Netlify folder
.netlify/
internal-docs/
temp/
config/example.conf
