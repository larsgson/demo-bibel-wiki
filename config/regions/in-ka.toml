# Karnataka — one of India's states/union territories, its own standalone
# regional entry point.   Subdomain: in-ka.demo.bibel.wiki   ·   Path: /r/in-ka
#
# Sibling file to config/regions/in.toml (the general India file, covering
# all 99 resolved India-wide languages) — see that file for the full
# methodology (Wikidata P17=India cross-checked against config/pkf-langs.json,
# data/source-catalog.json, config/licenses.json) and the broader exclusion
# reasoning (diaspora, sign languages, macrolanguage-vs-specific-variety,
# etc.) that also applies here. This file exists as a genuinely independent,
# standalone region — same schema, same treatment as config/regions/mx.toml
# — using "in-ka" as its own file code — the "in-" prefix is
# purely a naming/organizational convention (groups India's files
# together, and avoids any future ambiguity with an unrelated 2-letter
# country code), NOT a routing hierarchy: this is still a flat,
# independent region code, with no parent/child relationship to
# in.toml and no nested subdomain. Karnataka's own official ISO 3166-2
# subdivision code is IN-KA (a different, unrelated ID from
# this file's own "in-ka" code).
#
# Resolved languages in Karnataka (5 of Karnataka's share of the
# 99 India-wide languages):
#   - kan (Kannada): NT via helloao, OT via helloao
#   - tcy (Tulu): NT via dbt
#   - sle (Sholaga): NT via helloao
#   - xua (Alu Kurumba): NT via dbt
#   - xuj (Jennu Kurumba): NT via dbt
#
# Tulu (tcy), spoken by ~2M people, has no official recognition despite its size — included here regardless, same as every other resolving language.

code = "in-ka"
name = { en = "Karnataka" }
defaultLanguage = "kan"   # primary language when entering with none chosen
uiLanguage = "en"         # no Kannada UI locale exists (only
                          # src/locales/hi.ts does, and it's a stub) — English
                          # is used for the interface, same as every other
                          # India file until a local-script locale is built

# No separate trade language distinct from the state's own default here —
# unlike the multi-state in.toml, a single-state file doesn't need a
# separate "bridge" language pulled out of the list. English still covers
# that role implicitly (every reader has the global language picker).
tradeLanguages = ["eng"]

languages = [
  "kan", "tcy", "sle", "xua", "xuj",
]

# ── Groupings ────────────────────────────────────────────────────────────────
# A single flat group covering all of Karnataka's own languages — needed for
# the region landing page to render any language tiles at all (the page only
# ever shows languages that appear inside a grouping's group, never the flat
# `languages` list directly — see src/pages/r/[region]/index.astro). Single-
# state files don't need more than one bucket the way the multi-state
# in.toml's by-state grouping does.
[[grouping]]
id = "all"
navigable = false
name = { en = "Languages of Karnataka" }

  [[grouping.group]]
  slug = "all"
  name = { en = "Karnataka" }
  languages = ["kan", "tcy", "sle", "xua", "xuj"]

# ── Region landing page (/r/in-ka) ───────────────────────────────────────
# TODO: heroImage — no photo sourced yet (see mx-hero.jpg / id-hero.jpg for
# the pattern: a free-to-use Unsplash photo + heroCredit). Landing page falls
# back to a plain accent-color background until one is added.
#
# unlisted = true: kept off the main "/" homepage grid so India's 28 state
# files don't crowd out every other region there — still fully reachable at
# /r/in-ka/ and via the in-ka.demo.bibel.wiki redirect, same treatment
# za.toml uses for a different reason (thin content there; here it's
# homepage real estate). config/regions/in.toml remains the one India card
# shown on the homepage.

[landing]
unlisted = true
accentColor = "#FBC02D"
headline = { en = "The Bible in the languages of Karnataka" }
subhead  = { en = "Read and listen to Scripture in Karnataka's regional languages." }

# ── Picker "this region" tier ────────────────────────────────────────────────

[picker]
tierLabel = { en = "Languages of Karnataka" }
filterBy = "all"   # which grouping powers the picker filter chips

# ── Study + SEO ──────────────────────────────────────────────────────────────
# defaultLanguage (kan) is NOT in the fixed study-mode gate list (see
# src/lib/bw/study-languages.ts), so this state falls back to eng for
# defaultStudyLanguage, same gap za.toml documents for South Africa.

[study]
defaultStudyLanguage = "eng"

[seo]
host = "in-ka.demo.bibel.wiki"
